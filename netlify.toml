[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
```
4. Clique em **Commit changes** → **Commit changes**

---

## Passo 3 — Criar o `get-posts.js`

1. Clique em **Add file** → **Create new file**
2. No campo de nome, digite exatamente:
```
netlify/functions/get-posts.js
```
*(o GitHub cria as pastas automaticamente quando você digita a barra)*

3. Cole o conteúdo completo do arquivo `get-posts.js` que baixamos
4. Clique em **Commit changes** → **Commit changes**

---

## Passo 4 — Criar o `update-posts.js`

1. Clique em **Add file** → **Create new file**
2. No campo de nome, digite:
```
netlify/functions/update-posts.js
```
3. Cole o conteúdo completo do arquivo `update-posts.js`
4. Clique em **Commit changes** → **Commit changes**

---

## Passo 5 — Subir o `index.html`

1. Clique em **Add file** → **Upload files**
2. Arraste o arquivo `notion-grid-preview.html` para a área indicada
3. **Importante:** antes de commitar, renomeie o arquivo clicando nele e trocando o nome para `index.html`

> Se não conseguir renomear no upload, faça assim: após subir, clique no arquivo → ícone de lápis → mude o nome no topo para `index.html`

4. Clique em **Commit changes**

---

## Passo 6 — Verificar a estrutura

Ao final, seu repositório deve ter exatamente essa estrutura:
```
netlify.toml
index.html
netlify/
  functions/
    get-posts.js
    update-posts.js
